@model IEnumerable<StudentAffiairs.Models.Campus_Branches>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Campus_branch)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Campus_branch)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.campus_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.campus_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.campus_id })
        </td>
    </tr>
}

</table>
