@model IEnumerable<StudentAffiairs.Models.Inquiry>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UserId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.inquirry)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UserId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.inquirry)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Inquirery_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Inquirery_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Inquirery_id })
        </td>
    </tr>
}

</table>
