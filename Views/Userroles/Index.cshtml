@model IEnumerable<StudentAffiairs.Models.Userrole>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Student.UserName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Role.Role1)
        </th>

        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Student.UserName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Role.Role1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.userroleid }) |
            @Html.ActionLink("Details", "Details", new { id = item.userroleid }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.userroleid })
        </td>
    </tr>
}

</table>
